language: java
os: windows
#sudo: false
#dist: trusty

jdk:
  - oraclejdk8
  - oraclejdk9
  - openjdk8

notifications:
  email:
    on_success: never
    on_failure: always

addons:
  sonarcloud:
    enabled: true    
    organization: "tonikolaba-github"

script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=-Dsonar.organization=tonikolaba-github -Dsonar.login=d4a88a26b6214799b25d12778484e2fccc412d7e -Dsonar.branch=master
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar 

