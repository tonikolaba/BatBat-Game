language: java

sudo: required

jdk:
  #- oraclejdk9
  - oraclejdk8
  #- oraclejdk7


addons:
  sonarcloud:
    organization: "tonikolaba-github"
    token: $BatBat_Game
    install: true

script:
  #- sudo apt-get update && sudo apt-get install oracle-java8-installer
  #- java -version
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=-Dsonar.organiz
ation=tonikolaba-github -Dsonar.login=36f51fa745413db289321cc69597e068ac6dcb54 -Dsonar.branch=master



notifications:
  email:
    on_success: never
    on_failure: always

